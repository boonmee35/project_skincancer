# 🩺 Skin Cancer Detection & Forum Platform

ระบบแนะนำการวินิจฉัยมะเร็งผิวหนังด้วย AI พร้อมฟอรั่มการอภิปราย บทความ และแผงควบคุมผู้ดูแล

## 📋 ภาพรวมโปรเจ็กต์

โปรเจ็กต์นี้เป็นเว็บแอปพลิเคชันที่ช่วยให้ผู้ใช้สามารถ:
- 🔍 **วิเคราะห์รูปภาพผิวหนัง** โดยใช้โมเดล AI (MobileNetV2)
- 📚 **อ่านบทความ** เกี่ยวกับมะเร็งผิวหนังและการป้องกัน
- 💬 **มีส่วนร่วมในฟอรั่ม** เพื่อแลกเปลี่ยนความสูญสิย
- 📊 **ติดตามประวัติการวิเคราะห์** ของตัวเอง
- ⚙️ **แผงควบคุมผู้ดูแล** สำหรับจัดการเนื้อหา

---

## 🏗️ โครงสร้างเทคโนโลยี

### Frontend (React + Vite)
```
src/
├── pages/              # หน้าหลัก: วิเคราะห์, บทความ, ฟอรั่ม, ประวัติ
├── admin/              # หน้าแผงควบคุมผู้ดูแล
├── components/         # คอมโพเนนต์ร่วม
├── contexts/           # React Context สำหรับการรับรอง
└── assets/             # ไฟล์รูป, ไอคอน
```

**เทคโนโลยีหลัก:**
- React 19
- React Router DOM 7
- Vite (bundler)
- Tailwind CSS 4
- Axios (HTTP client)
- React Toastify (แจ้งเตือน)
- React Webcam (จับภาพจากกล้อง)
- CKEditor & TinyMCE (แก้ไขข้อความ)
- Recharts (แผนภูมิ)

### Backend (Node.js + Express)
```
server/
├── routes/             # API endpoints
├── controllers/        # ตรรกะเบื้องหลัง
├── middlewares/        # Middleware
└── uploads/            # ไฟล์อัปโหลด
```

**เทคโนโลยีหลัก:**
- Express 5
- MySQL2 (ฐานข้อมูล)
- JWT (การรับรอง)
- Bcryptjs (เข้ารหัสรหัสผ่าน)
- Multer (อัปโหลดไฟล์)
- Nodemon (auto-reload)

### AI Model (Python)
```
ai_model/
├── main.py             # API FastAPI สำหรับการทำนาย
├── train_MobileNetV2.py # โค้ดสำหรับฝึกโมเดล
├── mobilenetv2_skin_cancer.h5  # โมเดลที่ฝึกแล้ว
└── dataset/            # ชุดข้อมูลสำหรับฝึก
    ├── train/
    ├── val/
    └── test/
```

**เทคโนโลยีหลัก:**
- FastAPI (API server)
- TensorFlow/Keras (Deep Learning)
- MobileNetV2 (โมเดลสำหรับจำแนก)
- Scikit-learn (Machine Learning)
- PIL/Pillow (การประมวลผลรูปภาพ)

---

## 🚀 การติดตั้งและการรัน

### 1️⃣ Frontend Setup

```bash
npm install
npm run dev
```

จะเปิด http://localhost:5173

### 2️⃣ Backend Setup

```bash
cd server
npm install
```

สร้างไฟล์ `.env` ในโฟลเดอร์ `server/`:
```env
PORT=3000
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=your_password
DB_NAME=skin_cancer_db
JWT_SECRET=your_secret_key
```

จากนั้นรัน:
```bash
npm start
```

Backend จะทำงานที่ http://localhost:3000

### 3️⃣ AI Model Setup

```bash
cd ai_model
pip install -r requirements.txt
```

รัน FastAPI server:
```bash
python -m uvicorn main:app --reload --port 8000
```

API จะทำงานที่ http://localhost:8000

---

## 📊 API Endpoints

### Authentication
- `POST /api/auth/register` - สมัครสมาชิก
- `POST /api/auth/login` - เข้าสู่ระบบ

### Analysis
- `POST /api/analysis/upload` - อัปโหลดรูปและวิเคราะห์
- `GET /api/analysis/history` - ดูประวัติการวิเคราะห์

### Articles
- `GET /api/articles` - ดูบทความทั้งหมด
- `GET /api/articles/:id` - ดูรายละเอียดบทความ
- `POST /api/articles` - สร้างบทความ (Admin)

### Forum/Posts
- `GET /api/post` - ดูโพสต์ทั้งหมด
- `POST /api/post` - สร้างโพสต์ใหม่
- `POST /api/comment` - แสดงความคิดเห็น

### Admin
- `GET /api/dashboard` - สถิติแผงควบคุม
- `GET /api/users` - จัดการผู้ใช้
- `GET /api/categories` - จัดการหมวดหมู่

---

## 🤖 โมเดล AI

### MobileNetV2 Skin Cancer Classifier

**การจำแนก:**
- **BCC** (Basal Cell Carcinoma) - มะเร็งเซลล์ฐาน
- **MEL** (Melanoma) - เมลาโนมา
- **NV** (Nevus) - หล่อม
- **SCC** (Squamous Cell Carcinoma) - มะเร็งเซลล์สความัส
- **UNKNOWN** - ไม่รู้จัก

**ประสิทธิภาพ:**
- Input size: 224x224 pixels
- ฝึกด้วย Data Augmentation
- ใช้ transfer learning จาก ImageNet

---

## 📁 โครงสร้างของ Database

### ตาราหลัก:
- **users** - ข้อมูลผู้ใช้
- **analysis** - ประวัติการวิเคราะห์
- **articles** - บทความ
- **posts** - โพสต์ในฟอรั่ม
- **comments** - ความคิดเห็น
- **categories** - หมวดหมู่บทความ

---

## 🔐 ความปลอดภัย

- ✅ JWT Token สำหรับการรับรอง
- ✅ Bcryptjs สำหรับเข้ารหัสรหัสผ่าน
- ✅ CORS middleware
- ✅ Protected routes (Private/Admin routes)
- ✅ Role-based access control

---

## 🎯 คุณสมบัติหลัก

### 👥 ผู้ใช้ทั่วไป
- [x] สมัครสมาชิกและเข้าสู่ระบบ
- [x] อัปโหลดรูปภาพเพื่อวิเคราะห์
- [x] ดูผลการวิเคราะห์พร้อมสถิติความเชื่อมั่น
- [x] ติดตามประวัติการวิเคราะห์
- [x] อ่านบทความ
- [x] มีส่วนร่วมในฟอรั่ม
- [x] แสดงความคิดเห็นในโพสต์

### 🛡️ ผู้ดูแล
- [x] จัดการผู้ใช้
- [x] จัดการบทความและหมวดหมู่
- [x] จัดการโพสต์และความคิดเห็น
- [x] ดูสถิติการวิเคราะห์
- [x] จัดการการอัปโหลด

---

## 📦 ข้อกำหนดสภาวะแวดล้อม

### Frontend
- Node.js >= 18
- npm/yarn

### Backend
- Node.js >= 18
- MySQL 8.0+
- npm/yarn

### AI Model
- Python 3.10+
- pip

---

## 🚀 Build for Production

### Frontend
```bash
npm run build
```

### Backend
ปรับแต่ง `.env` สำหรับ production แล้วรัน

---

## 📝 License

ISC License

---

## 👨‍💻 Contributors

**Boonmee35**

---

## 📞 Support

หากมีปัญหา โปรดเปิด Issue หรือติดต่อทีมพัฒนา

---

**สถานะ:** 🚧 กำลังอยู่ระหว่างการพัฒนา